import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primaires Nyama
  static const Color primary = Color(0xFF1B4332);       // Vert Nyama
  static const Color secondary = Color(0xFFD4A017);     // Or Nyama

  // Fond et texte
  static const Color background = Color(0xFFFFFFFF);    // Blanc
  static const Color surface = Color(0xFFF9FAFB);       // Gris très léger
  static const Color textPrimary = Color(0xFF1A1A1A);   // Noir
  static const Color textSecondary = Color(0xFF6B7280); // Gris

  // États
  static const Color error = Color(0xFFDC2626);         // Rouge erreur
  static const Color success = Color(0xFF16A34A);       // Vert succès
  static const Color warning = Color(0xFFF59E0B);       // Orange

  // Nuances primaire
  static const Color primaryLight = Color(0xFF2D6A4F);
  static const Color primaryDark = Color(0xFF0D2B1E);

  // UI
  static const Color divider = Color(0xFFE5E7EB);
  static const Color shimmerBase = Color(0xFFE0E0E0);
  static const Color shimmerHighlight = Color(0xFFF5F5F5);
  static const Color cardShadow = Color(0x1A000000);
  static const Color overlay = Color(0x80000000);
}
